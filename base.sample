
SHELL_MODE=base_dev
TITLE=Development

MODE_PATH[0]="sbin"
MODE_PATH[1]="bin"
MODE_GIT_ITEM=".git"

setup() {
  echo "Advanced Shelling template setup function"
  return 0
}

teardown() {
  echo "Advanced Shelling template teardown function"
  return 0
}
